# Portfolio — planned

This feature module is intentionally empty in Phase 1. It exists as a
placeholder so the directory structure (components/, schemas/,
services/, types.ts) is ready when this feature's backend logic is
built in a later phase — no functionality is implemented here yet, and
none should be faked in the meantime. See docs/database-architecture.md
for how this area's data is expected to relate to the rest of the schema.
